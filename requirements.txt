# 개발 의존성 (pyproject.toml의 런타임 의존성에 추가)
# pip install -e .  →  프로덕션 설치
# pip install -r requirements.txt  →  개발/테스트 설치

# 패키지 자체 (편집 가능 모드) — 런타임 의존성은 pyproject.toml 참조
-e .

# 테스트 도구
pytest>=8
pytest-asyncio>=0.23
# httpx는 pyproject.toml runtime deps에 포함되어 -e . 로 설치됨
